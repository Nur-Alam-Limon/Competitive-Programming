#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll cnt=0
        char x;
        string s;
        cin>>s;
        for(ll i=0;i<s.size();i++)
        {
            if(a[i]=='A') cnta++;
            else if(a[i]=='B') cntb++;
            else if(a[i]=='C') cntc++;
        }
        if(cnta>=cntb && cnta>=cntc) x='A';
        else if(cntb>=cnta && cntb>=cntc) x='B';
        else x='C';
        for(ll i=0;i<s.size();i++)
        {
            if(a[i]==x) a[i]='(';
            else a[i]=')';
        }
        for(ll i=0;i<s.size();i++)
        {
            if(a)
        }
    }
    return 0;
}
