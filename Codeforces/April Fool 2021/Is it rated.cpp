#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    string s;
    while(getline(cin,s))
    {
        if(cin.eof()) break;
        printf("NO\n");
        fflush(stdout);
    }
    return 0;
}
