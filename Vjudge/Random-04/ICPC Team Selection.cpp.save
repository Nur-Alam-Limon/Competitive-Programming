#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n,x=0,y=0,z=0;
        cin>>n;
        x=n*3;
        ll a[x+5];
        for(ll i=0;i<x;i++) cin>>a[i];
        for(ll j=0;j<3;j+=3)
        {
            y=max(a[j],)
        }
    }
    return 0;
}
