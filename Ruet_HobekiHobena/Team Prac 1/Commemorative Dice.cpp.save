#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    ll a[8],b[8];
    for(ll i=0;i<6;i++) cin>>a[i];
    for(ll i=0;i<6;i++) cin>>b[i];
    sort(a,a+6);
    sort(b)
    return 0;
}

