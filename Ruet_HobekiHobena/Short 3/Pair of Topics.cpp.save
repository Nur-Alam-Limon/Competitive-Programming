#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
    ll n,ans=0,i=0,j=0,x=0;
    cin>>n;
    ll a[n+5],b[n+5],dif[n+5]={0};
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(i=0;i<n;i++)
    {
        cin>>b[i];
        dif[i]=a[i]-b[i];
    }
    sort(dif,dif+n);
    for(i=0;i<n-1;i++)
    {
        if(dif)
        x=lower_bound(dif.begin(),dif.end(),)
    }
    cout<<ans<<endl;
    return 0;
}

